#include <stdio.h>

/**
 * main - Entry Point
 * A program to run a line of printf function
 *
 * Return:0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
