#include "main.h"

/**
 * _abs - a function that computes the absolute value of an integer
 * @n: this is a inpu value to check
 *
 * Return: absolute value of int
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
